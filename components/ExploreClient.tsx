"use client";

import { ChinaMapExplorer } from "./ChinaMapExplorer";

/** 首页探索：全部景点目录 + 季节/长短/主题/地区筛选维度 → 路线攻略。
 *  UI 仍在 ChinaMapExplorer.tsx（历史文件名；已无地图选区 tab）。
 */
export function ExploreClient() {
  return <ChinaMapExplorer />;
}
